const isRegister=location.pathname.includes("register");document.getElementById("app").innerHTML=`<div class="container" style="max-width:520px"><div class="card"><h1>${isRegister?"Create your account":"Welcome back"}</h1><p class="muted">${isRegister?"Build your career profile and get personalized job matches.":"Sign in to your CareerJob dashboard."}</p><form id="f" class="form">${isRegister?'<div><label>Name</label><input class="input" name="name" required></div>':""}<div><label>Email</label><input class="input" type="email" name="email" required></div><div><label>Password</label><input class="input" type="password" name="password" minlength="6" required></div><button class="btn">${isRegister?"Create Account":"Login"}</button></form><p class="muted">${isRegister?'Already registered? <a href="/login.html">Login</a>':'New user? <a href="/register.html">Create account</a>'}</p><p><a href="/admin.html">Admin Login</a></p></div></div>`;document.getElementById("f").onsubmit=async e=>{e.preventDefault();try{const d=await api("/api/auth/"+(isRegister?"register":"login"),{method:"POST",body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});saveAuth(d);location.href="/dashboard.html"}catch(x){toast(x.message)}}